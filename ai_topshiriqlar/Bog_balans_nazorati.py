# Bog': balans nazorati
# Kurs: Dasturlash / IT
# Mavzu: Arifmetik operatorlar — + - * / // % ** va prioritet
# Ball: 100
# Aziz Academy — AI Topshiriq

import sys

balans = min_balans = 200000
chiqim = 0
for _ in range(int(input()))
amal = 0
balans += int(amal)
if amal[0] == '-':
    chiqim += 1
if balans < min_balans:
    min_balans = balans
print(f"{balans}\n{min_balans}\n{chiqim}")

balans = 0
min_balans = 0
chiqim = 0